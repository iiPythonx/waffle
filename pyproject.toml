[project]
name = "waffle"
version = "3.0.0"
description = "A 16-bit CPU with 16 KiB of RAM."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
waffleemu = "waffle.vm.__main__:main"
waffleasm = "waffle.asm.__main__:main"
